<%= form_with(model: phonetic) do |form| %>
  <% if phonetic.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(phonetic.errors.count, "error") %> prohibited this phonetic from being saved:</h2>

      <ul>
        <% phonetic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :phonetic, style: "display: block" %>
    <%= form.text_field :phonetic %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
